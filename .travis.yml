language: python
python:
  - "3.6"
  - "3.5"
  - "3.4"
  - "3.3"
  - "2.7"
install:
  - python setup.py install
  - pip install coveralls pytest-cov
script:
  - pytest --cov-report term-missing --cov=asciigraf tests/
after_success:
  - coveralls
branches:
  only:
    - master
